decl
  int num, sum;
  str mesg,mesg1;
enddecl

begin
    read(num);
    sum = 0;
    while (num != 0){
      sum = sum + num;
      read(num);
    }
    write("sum is:");
    write(sum);
    mesg = "good bye";
    write(mesg);
end;